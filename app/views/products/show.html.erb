<div class="banner-list card-category">
  <% if @product.photo.attached?  %>
    <%= cl_image_tag @product.photo.key, crop: :fill %>
  <% else %>
    <div>Imagem fixa</div>
  <% end %>
  <h1><%= @product.name %></h1>
  <h3><%= @product.price %></h3>
</div>
